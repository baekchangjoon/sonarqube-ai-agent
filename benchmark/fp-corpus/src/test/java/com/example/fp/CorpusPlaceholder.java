package com.example.fp;

/**
 * Placeholder so the corpus has a test root — the orchestrator's
 * scanner invocation expects src/test/java to exist. Not a real test;
 * the corpus measures triage, not coverage.
 */
class CorpusPlaceholder {
}
